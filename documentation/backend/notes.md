# this is just for catrina

Router → POST /recipes goes to recipeController.create  
Controller → Got it, here's the request — Chef, handle this  
Service → Validated, checked ownership — Pantry, I need this data  
Repository → Here's the SQL result
