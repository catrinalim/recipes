# recipes
App for storing recipes

## Starting App
In the frontend folder, run `npx expo start` to start dev.  
If it doesn't work, ensure your computer and device are on the same Wi-Fi network and run `npx expo start --tunnel`.  
